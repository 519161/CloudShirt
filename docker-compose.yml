version: '3.4'

services:
  eshopwebmvc:
    image: ${DOCKER_REGISTRY-}eshopwebmvc
    build:
      context: .
      dockerfile: src/Web/Dockerfile
  eshoppublicapi:
    image: ${DOCKER_REGISTRY-}eshoppublicapi
    build:
      context: .
      dockerfile: src/PublicApi/Dockerfile
  environment:
      - ConnectionStrings__DefaultConnection= "Server=mydbinstanceidentifier.cpugk80o4f95.us-east-1.rds.amazonaws.com,1433;User Id=admin;Password=adminpassword;Catalog=Microsoft.eShopOnWeb.Identity;"
